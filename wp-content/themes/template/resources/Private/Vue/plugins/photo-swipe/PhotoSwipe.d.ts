import { ComponentPublicInstance } from '@vue/runtime-core'

export declare type PhotoSwipePublicInstance = ComponentPublicInstance & {
	open: (event: Event, index: number) => void
	close: () => void
}
